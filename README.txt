GSEA Balloon Plot - Shinylive

Serve this complete directory using an HTTP(S) static web server.
Do not open index.html with file://. The browser will load R and required packages.
Only synthetic examples are bundled. Your own files are selected within the browser.
The application does not intentionally send selected input data to an external service.

Built with shinylive 0.5.0
Assets version 0.10.12
